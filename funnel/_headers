# Cloudflare Pages — HTTP headers
# https://developers.cloudflare.com/pages/configuration/headers/

/*
  X-Frame-Options: SAMEORIGIN
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=()

# CSS/JS change between deploys without changing filenames, so make browsers
# revalidate every time instead of serving a stale cached copy for 24h.
/assets/*.css
  Cache-Control: no-cache
/assets/*.js
  Cache-Control: no-cache

# Images rarely change — keep them cached but allow revalidation.
/assets/img/*
  Cache-Control: public, max-age=3600, must-revalidate

# Same rules for the v2 candidate redesign — its assets live under /v2/assets/,
# which the patterns above do not match, so without these a stale stylesheet
# survives a redeploy and v2 renders unstyled.
/v2/assets/*.css
  Cache-Control: no-cache
/v2/assets/*.js
  Cache-Control: no-cache
/v2/assets/img/*
  Cache-Control: public, max-age=3600, must-revalidate

# HTML pages should always be fresh.
/*.html
  Cache-Control: no-cache
